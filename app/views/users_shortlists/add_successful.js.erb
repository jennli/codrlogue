toastr.options = {
  "closeButton": true,
  "positionClass": "toast-bottom-right"
}
toastr.success('Added to shortlist successfully!');
$('#shortlist-button').html("<%= j link_to "Remove from Shortlist", users_shortlists_path(id: @user.id), remote: true, method: :delete %>");
